version: 2

models:
  - name: tableau_leader_scorecard
    columns:
      
      - name: employee_id
        tests:
          - not_null

      - name: metric
        tests:
          - accepted_values:
              values: ['Headcount','Hires','Terminations','Surveys','Trainings']